import { CurrencyconPipe } from './currencycon.pipe';

describe('CurrencyconPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyconPipe();
    expect(pipe).toBeTruthy();
  });
});
